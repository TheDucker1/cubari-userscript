Vibe-coded with gemini-cli so no security issue were taken into account, do not input any sensitive data.

run at manga website, get the image list with css selector (example: `div.page-chapter > img:first-of-type`, just ask an AI to spawn css rule using natural language) then it would spawn a cubari reader with the image, because cubarimoe doesn't support most of the aggregator sites.